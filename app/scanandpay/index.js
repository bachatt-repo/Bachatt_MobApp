import { View, Text } from "react-native";

export default function ScanAndPayPage() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Scan and Pay Page</Text>
    </View>
  );
}
